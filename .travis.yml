sudo: false

language: python

git:
  depth: 10
  submodules_depth: 10

matrix:
  include:
    - os: osx
      language: sh

    - os: windows
      language: sh
      before_install:
      - choco install python3
      - export PATH="/c/Python37:/c/Python37/Scripts:$PATH"
      
    - os: linux
      python: "2.7"

    - os: linux
      python: "3.6"

install: pip install --user .[tests]

script: python -m pytest -rsv

notifications:
  email: false